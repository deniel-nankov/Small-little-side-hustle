# ============================================================================
#  Development / CI dependencies — testing, linting, type-checking, security.
#  Installed on top of requirements.txt (see Makefile `install`).
# ============================================================================
-r requirements.txt

ruff==0.9.2
mypy==2.1.0
pytest==8.3.4
pytest-cov==6.0.0
pip-audit==2.10.1
bandit==1.8.0
types-python-dateutil==2.9.0.20241206
