#!/usr/bin/env bash
# One-command environment setup for yale-alpha-fund. (Milestone 1)
# Creates the venv and installs dependencies; see docs/DEPLOYMENT.md.
set -euo pipefail
echo "TODO: implement setup (python3 -m venv .venv && make install)"
