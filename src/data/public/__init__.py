"""Free public data sources (SEC EDGAR fundamentals + Yahoo prices).

Real, credential-free market data behind the same :class:`~src.data.source.base.DataSource`
interface, so signals can run on genuine point-in-time data while FactSet entitlement is
pending. Select with ``DATA_SOURCE=public``.
"""
